'use babel';

import AtomReferenceSeekerView from '../lib/atom-referenceseeker-view';

describe('AtomReferenceSeekerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
